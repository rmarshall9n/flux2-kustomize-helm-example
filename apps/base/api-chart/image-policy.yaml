---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: api-chart
  namespace: apps
spec:
  imageRepositoryRef:
    name: api-chart
  policy:
    semver:
      range: 0.0.x
